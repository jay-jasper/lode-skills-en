# spec change log — <project name>

> Lands at `docs/spec-changelog.md`
> Requirements change as you iterate; record every requirement-level change here so "docs are the single source of truth" stays traceable.
> Appended by lode-spec / self-evolution when writing back to the spec.

## <date> — <one-line summary>
- **What changed**:
- **Why** (source: user decision / signal evolution / slice back-pressure):
- **Impact**: do downstream design-brief / dev-plan need syncing:
