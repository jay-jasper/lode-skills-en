# Product-Spec change log — <project name>

> Lands at `.lode/<project>/Product-Spec-CHANGELOG.md`
> Requirements change as you iterate; record every requirement-level change here so "docs are the single source of truth" stays traceable.
> Appended by lode-spec / self-evolution when writing back to the Product-Spec.

## <date> — <one-line summary>
- **What changed**:
- **Why** (source: user decision / signal evolution / Face back-pressure):
- **Impact**: do downstream Design-Brief / DEV-PLAN need syncing:
